import { describe,expect,it } from 'vitest';import { demoRequests } from '../demo-data';import { filterRequests,formatTime,toCustomers } from './selectors';
describe('request selectors',()=>{it('makes active the owner action queue and preserves paid/history',()=>{expect(filterRequests(demoRequests,'','__active','').map(r=>r.status)).toEqual(['new','quote_ready']);expect(filterRequests(demoRequests,'','__archive','').map(r=>r.status)).toEqual(['confirmed','completed'])});it('keeps reconciliation in the action queue',()=>{const item={...demoRequests[2],status:'confirmed' as const,lifecycleEffects:[{id:'e',effectType:'wix_contact' as const,state:'reconciliation_required' as const,enabledSnapshot:false,attemptCount:1,maxAttempts:4,updatedAt:'2026-08-30'}]};expect(filterRequests([item],'','__active','')).toHaveLength(1)});it('combines repeat guests',()=>expect(toCustomers(demoRequests).length).toBeGreaterThan(0));it('formats stored 24-hour ride times with explicit AM and PM',()=>{expect(formatTime('13:45')).toBe('1:45 PM');expect(formatTime('00:15')).toBe('12:15 AM')})});
