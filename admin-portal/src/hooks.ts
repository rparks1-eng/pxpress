import { useCallback, useEffect, useState } from 'react';
import { getAnalytics, listEvents, listNotificationRequests, listRequests,type NotificationRequestProvenance } from './lib/repository';
import type { AnalyticsSnapshot, AuditEvent, RideRequest } from './types';
export function useRequests(){const [data,setData]=useState<RideRequest[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');const load=useCallback(()=>{setLoading(true);setError('');listRequests().then(setData).catch(e=>setError(e.message||'Unable to load requests.')).finally(()=>setLoading(false))},[]);useEffect(load,[load]);return{data,loading,error,reload:load}}
export function useNotificationRequests(){const [data,setData]=useState<RideRequest[]>([]),[provenance,setProvenance]=useState<NotificationRequestProvenance>('disconnected'),[loading,setLoading]=useState(true),[error,setError]=useState('');const load=useCallback(()=>{setLoading(true);setError('');listNotificationRequests().then(snapshot=>{setData(snapshot.data);setProvenance(snapshot.provenance)}).catch(e=>{setData([]);setProvenance('disconnected');setError(e.message||'Notifications are disconnected.')}).finally(()=>setLoading(false))},[]);useEffect(load,[load]);return{data,provenance,loading,error,reload:load}}
export function useEvents(requestId?:string){const [data,setData]=useState<AuditEvent[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');const load=useCallback(()=>{setLoading(true);listEvents(requestId).then(setData).catch(e=>setError(e.message||'Unable to load activity.')).finally(()=>setLoading(false))},[requestId]);useEffect(load,[load]);return{data,loading,error,reload:load}}
export function useAnalytics(){const [data,setData]=useState<AnalyticsSnapshot>({daily:[],pages:[],sources:[],services:[]}),[loading,setLoading]=useState(true),[error,setError]=useState('');const load=useCallback(()=>{setLoading(true);setError('');getAnalytics().then(setData).catch(e=>setError(e.message||'Unable to load analytics.')).finally(()=>setLoading(false))},[]);useEffect(load,[load]);return{data,loading,error,reload:load}}
