import { AlertTriangle, Inbox, LoaderCircle } from 'lucide-react';
export function StatePanel({kind,title,body,retry}:{kind:'loading'|'empty'|'error';title:string;body?:string;retry?:()=>void}){const Icon=kind==='loading'?LoaderCircle:kind==='error'?AlertTriangle:Inbox;return <section className={`state state-${kind}`} role={kind==='error'?'alert':'status'} aria-live={kind==='loading'?'polite':undefined} aria-busy={kind==='loading'}><Icon aria-hidden/><p className="eyebrow">{kind==='loading'?'One moment':kind==='error'?'Needs attention':'Nothing waiting'}</p><h2>{title}</h2>{body&&<p>{body}</p>}{retry&&<button type="button" className="button secondary" onClick={retry}>Try again</button>}</section>}
